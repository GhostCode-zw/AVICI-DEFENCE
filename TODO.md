# TODO

- [ ] Fix routing compilation errors
- [x] Update `src/App.jsx` to import `BrowserRouter`/`Routes`/`Route` and wrap app correctly.
  - [x] Update `src/pages/navbar.jsx` to import `Link` from `react-router-dom`.
- [ ] Ensure `react-router-dom` is listed in `package.json` and install dependencies.
  - [x] Run `npm run dev` (or `npm run build`) to confirm build passes.




